# Configurações locais do Claude Code (contêm caminhos e TOKENS locais — nunca versionar)
# Exceção: .claude/skills/ é versionado para compartilhar skills com a equipe
.claude/*
!.claude/skills/

# Segredos / credenciais (defesa)
*.env
.env*
*token*
*secret*
*credential*
*.key
*.pem

# Sistema operacional
Thumbs.db
Desktop.ini
.DS_Store

# Dependências
node_modules/
